import "package:flutter/material.dart";

/// Error view of the app
class ErrorView extends StatelessWidget {
  /// Error view of the app
  const ErrorView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Error"),
    );
  }
}
